#include <stdio.h>

int main(void)
{
	  int n;
	  int a[5];
	  int *p;

	  a[2] = 1024;
	  p = &n;
	  /*
	   * write your line of code here...
	   * Remember:
	   * u are not allowed to use a
	   */
	 printf("a[2] = %d\n", a[2]);
	return (0);
}	
